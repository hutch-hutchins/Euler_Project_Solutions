# Data Files

Store official Project Euler input files here when a problem needs them.

Conventions:

- Keep downloaded input files raw and unchanged.
- Name files by problem number, for example `p0022_names.txt`.
- Load fixed inputs from problem modules with `include_str!()`.
- Do not store generated outputs or caches here.
